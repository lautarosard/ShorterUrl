services:
  mongo:
    image: mongo:latest
    container_name: shorter_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:alpine
    container_name: shorter_redis
    ports:
      - "6379:6379"

volumes:
  mongo_data: